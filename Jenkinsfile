
pipeline {
  agent { any }
  stages {
    stage (‘Git’) {
      steps {
        def myname = "JENKINS"
	echo "Building repo for ${myname}"
 }
}
    stage (‘Build’) {
      steps {
       //Build the contents of repo
      
     }
}

  }
}
